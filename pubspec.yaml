name: esc_pos_bluetooth
description: The library allows to print receipts using an ESC/POS thermal Bluetooth printer.
version: 0.4.1
homepage: https://github.com/andrey-ushakov/esc_pos_bluetooth

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.7
  
  flutter_bluetooth_basic: 
    path: /Users/sumitkakshapati/Documents/work/rara/flutter_bluetooth_basic


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
